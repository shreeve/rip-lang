---
description: Rip reactive operators, context sharing, and bindings
globs: "{src/compiler.js,src/components.js,src/grammar/**/*.rip,**/*.rip}"
alwaysApply: false
---

# Reactivity

Rip reactivity is built into the language, not imported from a library.

## Operators

| Operator | Name | Output |
| --- | --- | --- |
| `=` | assign | `let x; x = value` |
| `:=` | state | `const x = __state(value)` |
| `~=` | computed | `const x = __computed(() => expr)` |
| `~>` | effect | `__effect(() => { ... })` |
| `=!` | readonly | `const x = value` |
| `offer` | context provide | state + `setContext(...)` |
| `accept` | context consume | `getContext(...)` |

The reactive runtime is embedded in `src/compiler.js` and only emitted when needed.

## `offer` / `accept`

These are context-sensitive keywords that only activate inside component bodies. Elsewhere they remain plain identifiers.

```coffee
export Tabs = component
  offer active := 'overview'

export TabContent = component
  accept active
```

`offer` can wrap `:=`, `~=`, `=`, or `=!`. The same reactive object flows through parent and child.

Implementation details:

- tokenized as `OFFER` / `ACCEPT` only inside component bodies
- handled via `classifyKeyword` override in `src/components.js`
- grammar lives in `ComponentLine`
- runtime uses existing `setContext`, `getContext`, and `_parent` chain

## Three-Tier State Model

| Tier | Scope | Mechanism | Example |
| --- | --- | --- | --- |
| Props | parent to child | `value <=> x`, `placeholder: "..."` | configuring a widget |
| Offer / Accept | ancestor to subtree | keywords | tabs sharing active state |
| Stash | app-wide | shared reactive proxy | auth state, theme |

## Two-Way Binding

```coffee
input value <=> @name
input type: "number", value <=> @age
input type: "checkbox", checked <=> @active
Dialog open <=> @showConfirm
Select value <=> @role
```

For HTML elements, `<=>` compiles to:

1. an effect that pushes signal state into the DOM
2. an event listener that writes DOM changes back into the signal

Smart auto-binding also works for some cases like `value: @name`.

### Component Signal Sharing

`<=>` on components is different from `<=>` on HTML elements.

The parent passes the signal via `__bind_propName__`. The child checks that prop first:

```javascript
this.checked = __state(props.__bind_checked__ ?? props.checked ?? false)
```

Because `__state()` passes through existing signals, parent and child share the same signal object. No event-based synchronization is needed.
