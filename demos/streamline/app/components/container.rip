# Container - responsive max-width wrapper with horizontal padding

export Container = component
  @narrow := false

  render
    .('px-5')
      .('mx-auto',
        if narrow then 'max-w-[940px]' else 'max-w-[1120px]')
        #content
