# Lab Results — Rip UI Demo

import { get, use, start } from '@rip-lang/api'
import { serve } from '@rip-lang/api/serve'

dir = import.meta.dir

use serve dir: dir, components: 'routes', includes: ['components'], title: 'Lab Results'

get '/css/*'    -> @send "#{dir}/css/#{@req.path.slice(5)}"
get '/images/*' -> @send "#{dir}/images/#{@req.path.slice(8)}"
get '/*'        -> @send "#{dir}/index.html", 'text/html; charset=UTF-8'

start!
