# Home page - redirects to bookings or auth

export Home = component

  beforeMount: ->
    try
      res = await fetch('/api/user')
      data = await res.json()
      if data.user
        @router.replace '/bookings'
      else
        @router.replace '/auth'
    catch
      @router.replace '/auth'

  render
    .('flex items-center justify-center py-20')
      p.('text-tertiary') "Loading..."
