import { Input } from "../components/input.rip"
import { Button } from "../components/button.rip"

export App = component
  render
    div class: "flex min-h-dvh items-center justify-center bg-gray-50 pb-8 md:py-10 lg:py-12"
      div class: "rounded-2xl shadow-lg/6 border border-gray-200/70 bg-white p-6 pt-8 xs:px-8 md:p-11"
        div class: "xs:w-80"
          Form title: "Sign in"

export Form = component
  @title:: string := ""
  email := ""
  password := ""
  loading := false

  onSubmit: (e) ->
    e.preventDefault()
    loading = true
    p "Signing in: #{email}"
    loading = false

  render
    form @submit: @onSubmit
      div class: "text-center text-2xl leading-none font-semibold"
        title
      div class: "mt-6 flex flex-col gap-y-6"
        Input
          label: "Email"
          value <=> email
          placeholder: "me@example.com"
          type: "email"
          autocomplete: "email"
        Input
          label: "Password"
          value <=> password
          placeholder: "••••••••"
          type: "password"
          autocomplete: "current-password"
      Button
        variant: "primary"
        type: "submit"
        loading: loading
        classes: "mt-8 w-full"
        "Sign In"
