// Predictive Recursive Descent Parser generated by Solar 1.4.0 (PRD mode)

// Terminal symbol constants (for lookahead dispatch)
const SYM_EOF = 1, SYM_TERMINATOR = 6, SYM_STATEMENT = 11, SYM_DEF = 27, SYM_CALL_START = 29, SYM_CALL_END = 31, SYM_YIELD = 35, SYM_INDENT = 36, SYM_OUTDENT = 38, SYM_FROM = 39, SYM_IDENTIFIER = 40, SYM_PROPERTY = 42, SYM_NUMBER = 44, SYM_STRING = 46, SYM_STRING_START = 47, SYM_STRING_END = 49, SYM_INTERPOLATION_START = 51, SYM_INTERPOLATION_END = 52, SYM_REGEX = 54, SYM_REGEX_START = 55, SYM_REGEX_END = 57, SYM_COMMA = 59, SYM_JS = 61, SYM_UNDEFINED = 62, SYM_NULL = 63, SYM_BOOL = 64, SYM_INFINITY = 65, SYM_NAN = 66, SYM_ASSIGN = 68, SYM_COLON = 72, SYM_LBRACKET = 75, SYM_RBRACKET = 76, SYM_AT = 77, SYM_ELLIPSIS = 78, SYM_SUPER = 83, SYM_DYNAMIC_IMPORT = 86, SYM_DOT = 87, SYM_OPT_DOT = 88, SYM_PROTO = 89, SYM_OPT_PROTO = 90, SYM_INDEX_START = 91, SYM_INDEX_END = 92, SYM_INDEX_SOAK = 93, SYM_RETURN = 94, SYM_PARAM_START = 95, SYM_PARAM_END = 96, SYM_THIN_ARROW = 98, SYM_FAT_ARROW = 99, SYM_ES6_OPTIONAL_INDEX = 107, SYM_NEW_TARGET = 111, SYM_IMPORT_META = 112, SYM_LBRACE = 113, SYM_FOR = 114, SYM_FOROF = 116, SYM_RBRACE = 117, SYM_WHEN = 118, SYM_OWN = 119, SYM_CLASS = 121, SYM_EXTENDS = 122, SYM_IMPORT = 123, SYM_AS = 128, SYM_DEFAULT = 129, SYM_IMPORT_ALL = 130, SYM_EXPORT = 131, SYM_EXPORT_ALL = 133, SYM_ES6_OPTIONAL_CALL = 135, SYM_FUNC_EXIST = 136, SYM_THIS = 138, SYM_RANGE_INCL = 143, SYM_TRY = 148, SYM_FINALLY = 150, SYM_CATCH = 151, SYM_THROW = 152, SYM_LPAREN = 153, SYM_RPAREN = 154, SYM_WHILE = 156, SYM_UNTIL = 157, SYM_LOOP = 159, SYM_FORIN = 160, SYM_BY = 161, SYM_FORFROM = 162, SYM_AWAIT = 163, SYM_SWITCH = 166, SYM_ELSE = 168, SYM_LEADING_WHEN = 170, SYM_IF = 172, SYM_UNLESS = 174, SYM_POST_IF = 175, SYM_POST_UNLESS = 176, SYM_UNARY = 177, SYM_DO = 178, SYM_DO_IIFE = 179, SYM_UNARY_MATH = 180, SYM_MINUS = 181, SYM_PLUS = 182, SYM_DEC = 183, SYM_INC = 184, SYM_QUESTION = 185, SYM_MATH = 186, SYM_POWER = 187, SYM_SHIFT = 188, SYM_COMPARE = 189, SYM_BITAND = 190, SYM_BITXOR = 191, SYM_BITOR = 192, SYM_AND = 193, SYM_OR = 194, SYM_NULLISH = 195, SYM_OTHERWISE = 196, SYM_RELATION = 197, SYM_TERNARY_Q = 198, SYM_COMPOUND_ASSIGN = 199;

// Symbol ID map (lexer returns strings, parser needs numeric IDs)
const SYMBOL_IDS = {"$accept": 0, "$end": 1, "error": 2, "Root": 3, "Body": 4, "Line": 5, "TERMINATOR": 6, "Expression": 7, "ExpressionLine": 8, "Statement": 9, "Return": 10, "STATEMENT": 11, "Import": 12, "Export": 13, "Value": 14, "Code": 15, "Operation": 16, "Assign": 17, "If": 18, "Try": 19, "While": 20, "For": 21, "Switch": 22, "Class": 23, "Throw": 24, "Yield": 25, "Def": 26, "DEF": 27, "Identifier": 28, "CALL_START": 29, "ParamList": 30, "CALL_END": 31, "Block": 32, "CodeLine": 33, "OperationLine": 34, "YIELD": 35, "INDENT": 36, "Object": 37, "OUTDENT": 38, "FROM": 39, "IDENTIFIER": 40, "Property": 41, "PROPERTY": 42, "AlphaNumeric": 43, "NUMBER": 44, "String": 45, "STRING": 46, "STRING_START": 47, "Interpolations": 48, "STRING_END": 49, "InterpolationChunk": 50, "INTERPOLATION_START": 51, "INTERPOLATION_END": 52, "Regex": 53, "REGEX": 54, "REGEX_START": 55, "Invocation": 56, "REGEX_END": 57, "RegexWithIndex": 58, ",": 59, "Literal": 60, "JS": 61, "UNDEFINED": 62, "NULL": 63, "BOOL": 64, "INFINITY": 65, "NAN": 66, "Assignable": 67, "=": 68, "AssignObj": 69, "ObjAssignable": 70, "ObjRestValue": 71, ":": 72, "SimpleObjAssignable": 73, "ThisProperty": 74, "[": 75, "]": 76, "@": 77, "...": 78, "ObjSpreadExpr": 79, "Parenthetical": 80, "Super": 81, "This": 82, "SUPER": 83, "OptFuncExist": 84, "Arguments": 85, "DYNAMIC_IMPORT": 86, ".": 87, "?.": 88, "::": 89, "?::": 90, "INDEX_START": 91, "INDEX_END": 92, "INDEX_SOAK": 93, "RETURN": 94, "PARAM_START": 95, "PARAM_END": 96, "FuncGlyph": 97, "->": 98, "=>": 99, "OptComma": 100, "Param": 101, "ParamVar": 102, "Array": 103, "Splat": 104, "SimpleAssignable": 105, "Slice": 106, "ES6_OPTIONAL_INDEX": 107, "Range": 108, "DoIife": 109, "MetaProperty": 110, "NEW_TARGET": 111, "IMPORT_META": 112, "{": 113, "FOR": 114, "ForVariables": 115, "FOROF": 116, "}": 117, "WHEN": 118, "OWN": 119, "AssignList": 120, "CLASS": 121, "EXTENDS": 122, "IMPORT": 123, "ImportDefaultSpecifier": 124, "ImportNamespaceSpecifier": 125, "ImportSpecifierList": 126, "ImportSpecifier": 127, "AS": 128, "DEFAULT": 129, "IMPORT_ALL": 130, "EXPORT": 131, "ExportSpecifierList": 132, "EXPORT_ALL": 133, "ExportSpecifier": 134, "ES6_OPTIONAL_CALL": 135, "FUNC_EXIST": 136, "ArgList": 137, "THIS": 138, "Elisions": 139, "ArgElisionList": 140, "OptElisions": 141, "RangeDots": 142, "..": 143, "Arg": 144, "ArgElision": 145, "Elision": 146, "SimpleArgs": 147, "TRY": 148, "Catch": 149, "FINALLY": 150, "CATCH": 151, "THROW": 152, "(": 153, ")": 154, "WhileSource": 155, "WHILE": 156, "UNTIL": 157, "Loop": 158, "LOOP": 159, "FORIN": 160, "BY": 161, "FORFROM": 162, "AWAIT": 163, "ForValue": 164, "ForVar": 165, "SWITCH": 166, "Whens": 167, "ELSE": 168, "When": 169, "LEADING_WHEN": 170, "IfBlock": 171, "IF": 172, "UnlessBlock": 173, "UNLESS": 174, "POST_IF": 175, "POST_UNLESS": 176, "UNARY": 177, "DO": 178, "DO_IIFE": 179, "UNARY_MATH": 180, "-": 181, "+": 182, "--": 183, "++": 184, "?": 185, "MATH": 186, "**": 187, "SHIFT": 188, "COMPARE": 189, "&": 190, "^": 191, "|": 192, "&&": 193, "||": 194, "??": 195, "!?": 196, "RELATION": 197, "SPACE?": 198, "COMPOUND_ASSIGN": 199};

// Token names for error messages
const TOKEN_NAMES = {2: "error", 6: "TERMINATOR", 11: "STATEMENT", 27: "DEF", 29: "CALL_START", 31: "CALL_END", 35: "YIELD", 36: "INDENT", 38: "OUTDENT", 39: "FROM", 40: "IDENTIFIER", 42: "PROPERTY", 44: "NUMBER", 46: "STRING", 47: "STRING_START", 49: "STRING_END", 51: "INTERPOLATION_START", 52: "INTERPOLATION_END", 54: "REGEX", 55: "REGEX_START", 57: "REGEX_END", 59: ",", 61: "JS", 62: "UNDEFINED", 63: "NULL", 64: "BOOL", 65: "INFINITY", 66: "NAN", 68: "=", 72: ":", 75: "[", 76: "]", 77: "@", 78: "...", 83: "SUPER", 86: "DYNAMIC_IMPORT", 87: ".", 88: "?.", 89: "::", 90: "?::", 91: "INDEX_START", 92: "INDEX_END", 93: "INDEX_SOAK", 94: "RETURN", 95: "PARAM_START", 96: "PARAM_END", 98: "->", 99: "=>", 107: "ES6_OPTIONAL_INDEX", 111: "NEW_TARGET", 112: "IMPORT_META", 113: "{", 114: "FOR", 116: "FOROF", 117: "}", 118: "WHEN", 119: "OWN", 121: "CLASS", 122: "EXTENDS", 123: "IMPORT", 128: "AS", 129: "DEFAULT", 130: "IMPORT_ALL", 131: "EXPORT", 133: "EXPORT_ALL", 135: "ES6_OPTIONAL_CALL", 136: "FUNC_EXIST", 138: "THIS", 143: "..", 148: "TRY", 150: "FINALLY", 151: "CATCH", 152: "THROW", 153: "(", 154: ")", 156: "WHILE", 157: "UNTIL", 159: "LOOP", 160: "FORIN", 161: "BY", 162: "FORFROM", 163: "AWAIT", 166: "SWITCH", 168: "ELSE", 170: "LEADING_WHEN", 172: "IF", 174: "UNLESS", 175: "POST_IF", 176: "POST_UNLESS", 177: "UNARY", 178: "DO", 179: "DO_IIFE", 180: "UNARY_MATH", 181: "-", 182: "+", 183: "--", 184: "++", 185: "?", 186: "MATH", 187: "**", 188: "SHIFT", 189: "COMPARE", 190: "&", 191: "^", 192: "|", 193: "&&", 194: "||", 195: "??", 196: "!?", 197: "RELATION", 198: "SPACE?", 199: "COMPOUND_ASSIGN"};


// Operator precedence table (higher = binds tighter)
const OPERATOR_PRECEDENCE = {
  [SYM_OR]: 1,
  [SYM_AND]: 2,
  [SYM_NULLISH]: 3,
  [SYM_OTHERWISE]: 3,
  [SYM_BITOR]: 4,
  [SYM_BITXOR]: 5,
  [SYM_BITAND]: 6,
  [SYM_COMPARE]: 7,
  [SYM_RELATION]: 8,
  [SYM_SHIFT]: 9,
  [SYM_PLUS]: 10,
  [SYM_MINUS]: 10,
  [SYM_MATH]: 11,
  [SYM_POWER]: 12
};

// Operator associativity (default is 'left')
const OPERATOR_ASSOCIATIVITY = {
  [SYM_POWER]: 'right',  // 2**3**4 = 2**(3**4)
};


const parser = {
  lexer: null,
  la: null,          // Current lookahead token {id, value}
  peekToken: null,   // Buffered token for _peek()

  parse(input) {
    this.lexer.setInput(input);
    this.peekToken = null;
    this.la = this._advance();
    return this.parseRoot();
  },

  _match(expected) {
    if (this.la.id !== expected) {
      this._error([expected], this.la.id);
    }
    const tok = this.la.value;  // Return token VALUE (String object with metadata!)
    this.la = this._advance();
    return tok;
  },

  _peek() {
    // Buffer next token if not already buffered
    if (!this.peekToken) {
      let id = this.lexer.lex();
      if (id === null || id === undefined) return null;
      // Convert string tag to numeric ID
      if (typeof id !== 'number') {
        id = SYMBOL_IDS[id] || id;
      }
      this.peekToken = {id, value: this.lexer.yytext};
    }
    return this.peekToken ? this.peekToken.id : null;
  },

  _advance() {
    // Return buffered token if available, else get from lexer
    if (this.peekToken) {
      const tok = this.peekToken;
      this.peekToken = null;
      return tok;
    }

    // Get next token from lexer (returns string tag like "IDENTIFIER")
    let id = this.lexer.lex();
    if (id === 1 || id === null) return {id: 1, value: null};  // EOF

    // Convert string tag to numeric ID
    if (typeof id !== 'number') {
      id = SYMBOL_IDS[id] || id;
    }

    // CRITICAL: lexer.yytext can be String object with metadata!
    return {id, value: this.lexer.yytext};
  },

  _error(expected, got) {
    const expectedNames = expected.map(id => TOKEN_NAMES[id] || id).join(', ');
    const gotName = TOKEN_NAMES[got] || got;
    throw new Error(`Parse error: expected ${expectedNames}, got ${gotName}`);
  },

  // Parse functions follow...

  parseRoot() {
    switch (this.la.id) {
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_WHILE: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_SWITCH: case SYM_IF: case SYM_UNLESS: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC:
      return ["program", ...this.parseBody()];
    default: return ["program"];
    }
  },

  parseBody() {
    // Left-recursive: Body → Line | Body TERMINATOR Line
    const items = [this.parseLine()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_TERMINATOR) {
      this._match(SYM_TERMINATOR);
      // FOLLOW(Body) = {$end, TERMINATOR, OUTDENT, INTERPOLATION_END, )}
      if (this.la.id === SYM_EOF || this.la.id === SYM_OUTDENT || this.la.id === SYM_INTERPOLATION_END || this.la.id === SYM_RPAREN) break;
      items.push(this.parseLine());
    }

    return items;
  },

  parseLine() {
    switch (this.la.id) {
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_WHILE: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_SWITCH: case SYM_IF: case SYM_UNLESS: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC:
      return this.parseExpression();
    case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE:
      return this.parseExpressionLine();
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseValue() {
    switch (this.la.id) {
    case SYM_LBRACE: /* ERROR: Assignable should have been inlined */
    case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN:
      return this.parseLiteral();
    case SYM_LPAREN: return this.parseParenthetical();
    case SYM_LBRACKET: return this.parseRange();
    case SYM_IDENTIFIER: case SYM_DYNAMIC_IMPORT: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW:
      return this.parseInvocation();
    case SYM_DO_IIFE: return this.parseDoIife();
    case SYM_AT: case SYM_THIS: return this.parseThis();
    case SYM_SUPER: return this.parseSuper();
    case SYM_NEW_TARGET: case SYM_IMPORT_META: return this.parseMetaProperty();
    default: this._error([40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 95, 98, 99, 111, 112, 113, 138, 153, 179], this.la.id);
    }
  },

  parseIdentifier() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: return this._match(40);
    default: this._error([40], this.la.id);
    }
  },

  parseProperty() {
    switch (this.la.id) {
    case SYM_PROPERTY: return this._match(42);
    default: this._error([42], this.la.id);
    }
  },

  parseThisProperty() {
    switch (this.la.id) {
    case SYM_AT: {
      this._match(77);
            const tok2 = this.parseProperty();
            return [".", "this", tok2];
    }
    default: this._error([77], this.la.id);
    }
  },

  parseLiteral() {
    switch (this.la.id) {
    case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: return this.parseAlphaNumeric();
    case SYM_JS: return this._match(61);
    case SYM_REGEX: case SYM_REGEX_START: return this.parseRegex();
    case SYM_UNDEFINED: return "undefined";
    case SYM_NULL: return "null";
    case SYM_BOOL: return this._match(64);
    case SYM_INFINITY: return this._match(65);
    case SYM_NAN: return this._match(66);
    default: this._error([44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66], this.la.id);
    }
  },

  parseAlphaNumeric() {
    switch (this.la.id) {
    case SYM_NUMBER: return this._match(44);
    case SYM_STRING: case SYM_STRING_START: return this.parseString();
    default: this._error([44, 46, 47], this.la.id);
    }
  },

  parseString() {
    switch (this.la.id) {
    case SYM_STRING: return this._match(46);
    case SYM_STRING_START: {
      this._match(47);
            const tok2 = this.parseInterpolations();
            this._match(49);
            return ["str", ...tok2];
    }
    default: this._error([46, 47], this.la.id);
    }
  },

  parseRegex() {
    switch (this.la.id) {
    case SYM_REGEX: return this._match(54);
    case SYM_REGEX_START: {
      this._match(55);
            const tok2 = this.parseInvocation();
            this._match(57);
            return ["regex", tok2];
    }
    default: this._error([54, 55], this.la.id);
    }
  },

  parseParenthetical() {
    switch (this.la.id) {
    case SYM_LPAREN: {
      const tok1 = this._match(153);
      const next = this._peek();

      if (next === SYM_INDENT) {
        this._match(36);
        const tok3 = this.parseBody();
        this._match(38);
        this._match(154);
        return tok3.length === tok1 ? tok3[0] : tok3;
      } else {
        const tok2 = this.parseBody();
        this._match(154);
        return tok2.length === tok1 ? tok2[0] : tok2;
      }
    }
    default: this._error([153], this.la.id);
    }
  },

  parseRange() {
    switch (this.la.id) {
    case SYM_LBRACKET: {
      this._match(75);
            const tok2 = this.parseExpression();
            const tok3 = this.parseRangeDots();
            const tok4 = this.parseExpression();
            this._match(76);
            return [tok3, tok2, tok4];
    }
    default: this._error([75], this.la.id);
    }
  },

  parseInvocation() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_THIS: case SYM_LPAREN: case SYM_DO_IIFE: {
      const tok1 = this.parseValue();
            this.parseOptFuncExist();
            const tok3 = this.parseString();
            return ["tagged-template", tok1, tok3];
    }
    default: this._error([40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 95, 98, 99, 111, 112, 113, 138, 153, 179], this.la.id);
    }
  },

  parseDoIife() {
    switch (this.la.id) {
    case SYM_DO_IIFE: {
      this._match(179);
            const tok2 = this.parseCode();
            return ["do-iife", tok2];
    }
    default: this._error([179], this.la.id);
    }
  },

  parseThis() {
    switch (this.la.id) {
    case SYM_THIS: return "this";
    case SYM_AT: return "this";
    default: this._error([77, 138], this.la.id);
    }
  },

  parseSuper() {
    switch (this.la.id) {
    case SYM_SUPER: {
      this._match(83);
      const next = this._peek();

      if (next === SYM_DOT) {
        this._match(87);
        const tok3 = this.parseProperty();
        return [".", "super", tok3];
      } else if (next === SYM_INDEX_START) {
        this._match(91);
        const tok3 = this.parseExpression();
        this._match(92);
        return ["[]", "super", tok3];
      } else {
        this._match(91);
        this._match(36);
        const tok4 = this.parseExpression();
        this._match(38);
        this._match(92);
        return ["[]", "super", tok4];
      }
    }
    default: this._error([83], this.la.id);
    }
  },

  parseMetaProperty() {
    switch (this.la.id) {
    case SYM_NEW_TARGET: {
      this._match(111);
            this._match(87);
            const tok3 = this.parseProperty();
            return [".", "new", tok3];
    }
    case SYM_IMPORT_META: {
      this._match(112);
            this._match(87);
            const tok3 = this.parseProperty();
            return [".", "import", tok3];
    }
    default: this._error([111, 112], this.la.id);
    }
  },

  parseArray() {
    switch (this.la.id) {
    case SYM_LBRACKET: {
      this._match(75);
      const next = this._peek();

      if (next === SYM_RBRACKET) {
        this._match(76);
        return ["array"];
      } else if (next === SYM_COMMA) {
        const tok2 = this.parseElisions();
        this._match(76);
        return ["array", ...tok2];
      } else {
        const tok2 = this.parseArgElisionList();
        const tok3 = this.parseOptElisions();
        this._match(76);
        return ["array", ...tok2, ...tok3];
      }
    }
    default: this._error([75], this.la.id);
    }
  },

  parseObject() {
    switch (this.la.id) {
    case SYM_LBRACE: {
      this._match(113);
      const next = this._peek();

      if ((next === SYM_IDENTIFIER || next === SYM_PROPERTY || next === SYM_AT || next === SYM_LBRACKET || next === SYM_NUMBER || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseObjAssignable();
        this._match(72);
        const tok4 = this.parseExpression();
        this._match(114);
        const tok6 = this.parseForVariables();
        this._match(116);
        const tok8 = this.parseExpression();
        this.parseOptComma();
        this._match(117);
        return ["object-comprehension", tok2, tok4, [["for-of", tok6, tok8, false]], []];
      } else if ((next === SYM_IDENTIFIER || next === SYM_PROPERTY || next === SYM_AT || next === SYM_LBRACKET || next === SYM_NUMBER || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseObjAssignable();
        this._match(72);
        const tok4 = this.parseExpression();
        this._match(114);
        const tok6 = this.parseForVariables();
        this._match(116);
        const tok8 = this.parseExpression();
        this._match(118);
        const tok10 = this.parseExpression();
        this.parseOptComma();
        this._match(117);
        return ["object-comprehension", tok2, tok4, [["for-of", tok6, tok8, false]], [tok10]];
      } else if ((next === SYM_IDENTIFIER || next === SYM_PROPERTY || next === SYM_AT || next === SYM_LBRACKET || next === SYM_NUMBER || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseObjAssignable();
        this._match(72);
        const tok4 = this.parseExpression();
        this._match(114);
        this._match(119);
        const tok7 = this.parseForVariables();
        this._match(116);
        const tok9 = this.parseExpression();
        this.parseOptComma();
        this._match(117);
        return ["object-comprehension", tok2, tok4, [["for-of", tok7, tok9, true]], []];
      } else if ((next === SYM_IDENTIFIER || next === SYM_PROPERTY || next === SYM_AT || next === SYM_LBRACKET || next === SYM_NUMBER || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseObjAssignable();
        this._match(72);
        const tok4 = this.parseExpression();
        this._match(114);
        this._match(119);
        const tok7 = this.parseForVariables();
        this._match(116);
        const tok9 = this.parseExpression();
        this._match(118);
        const tok11 = this.parseExpression();
        this.parseOptComma();
        this._match(117);
        return ["object-comprehension", tok2, tok4, [["for-of", tok7, tok9, true]], [tok11]];
      } else {
        const tok2 = this.parseAssignList();
        this.parseOptComma();
        this._match(117);
        return ["object", ...tok2];
      }
    }
    default: this._error([113], this.la.id);
    }
  },

  parseBlock() {
    switch (this.la.id) {
    case SYM_INDENT: {
      this._match(36);
      const next = this._peek();

      if (next === SYM_OUTDENT) {
        this._match(38);
        return ["block"];
      } else {
        const tok2 = this.parseBody();
        this._match(38);
        return ["block", ...tok2];
      }
    }
    default: this._error([36], this.la.id);
    }
  },

  parseReturn() {
    switch (this.la.id) {
    case SYM_RETURN: {
      this._match(94);
      const next = this._peek();

      if (next === SYM_INDENT) {
        this._match(36);
        const tok3 = this.parseObject();
        this._match(38);
        return ["return", tok3];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START || next === SYM_UNARY || next === SYM_DO || next === SYM_UNARY_MATH || next === SYM_MINUS || next === SYM_PLUS || next === SYM_AWAIT || next === SYM_DEC || next === SYM_INC || next === SYM_TRY || next === SYM_FOR || next === SYM_SWITCH || next === SYM_CLASS || next === SYM_THROW || next === SYM_YIELD || next === SYM_DEF || next === SYM_IF || next === SYM_UNLESS || next === SYM_RETURN || next === SYM_STATEMENT || next === SYM_IMPORT || next === SYM_EXPORT || next === SYM_WHILE || next === SYM_UNTIL || next === SYM_LOOP)) {
        const tok2 = this.parseExpression();
        return ["return", tok2];
      } else {
        return ["return"];
      }
    }
    default: this._error([94], this.la.id);
    }
  },

  parseYield() {
    switch (this.la.id) {
    case SYM_YIELD: {
      this._match(35);
      const next = this._peek();

      if (next === SYM_INDENT) {
        this._match(36);
        const tok3 = this.parseObject();
        this._match(38);
        return ["yield", tok3];
      } else if (next === SYM_FROM) {
        this._match(39);
        const tok3 = this.parseExpression();
        return ["yield-from", tok3];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START || next === SYM_UNARY || next === SYM_DO || next === SYM_UNARY_MATH || next === SYM_MINUS || next === SYM_PLUS || next === SYM_AWAIT || next === SYM_DEC || next === SYM_INC || next === SYM_TRY || next === SYM_FOR || next === SYM_SWITCH || next === SYM_CLASS || next === SYM_THROW || next === SYM_YIELD || next === SYM_DEF || next === SYM_IF || next === SYM_UNLESS || next === SYM_RETURN || next === SYM_STATEMENT || next === SYM_IMPORT || next === SYM_EXPORT || next === SYM_WHILE || next === SYM_UNTIL || next === SYM_LOOP)) {
        const tok2 = this.parseExpression();
        return ["yield", tok2];
      } else {
        return ["yield"];
      }
    }
    default: this._error([35], this.la.id);
    }
  },

  parseRangeDots() {
    switch (this.la.id) {
    case SYM_RANGE_INCL: return "..";
    case SYM_ELLIPSIS: return "...";
    default: this._error([78, 143], this.la.id);
    }
  },

  parseImport() {
    switch (this.la.id) {
    case SYM_IMPORT: {
      this._match(123);
      const next = this._peek();

      if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseImportDefaultSpecifier();
        this._match(39);
        const tok4 = this.parseString();
        return ["import", tok2, tok4];
      } else if (next === SYM_IMPORT_ALL) {
        const tok2 = this.parseImportNamespaceSpecifier();
        this._match(39);
        const tok4 = this.parseString();
        return ["import", tok2, tok4];
      } else if (next === SYM_LBRACE) {
        this._match(113);
        this._match(117);
        this._match(39);
        const tok5 = this.parseString();
        return ["import", "{}", tok5];
      } else if (next === SYM_LBRACE) {
        this._match(113);
        const tok3 = this.parseImportSpecifierList();
        this.parseOptComma();
        this._match(117);
        this._match(39);
        const tok7 = this.parseString();
        return ["import", tok3, tok7];
      } else if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseImportDefaultSpecifier();
        this._match(59);
        const tok4 = this.parseImportNamespaceSpecifier();
        this._match(39);
        const tok6 = this.parseString();
        return ["import", [tok2, tok4], tok6];
      } else if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseImportDefaultSpecifier();
        this._match(59);
        this._match(113);
        const tok5 = this.parseImportSpecifierList();
        this.parseOptComma();
        this._match(117);
        this._match(39);
        const tok9 = this.parseString();
        return ["import", [tok2, tok5], tok9];
      } else {
        const tok2 = this.parseString();
        return ["import", "{}", tok2];
      }
    }
    default: this._error([123], this.la.id);
    }
  },

  parseExport() {
    switch (this.la.id) {
    case SYM_EXPORT: {
      this._match(131);
      const next = this._peek();

      if (next === SYM_LBRACE) {
        this._match(113);
        this._match(117);
        return ["export", "{}"];
      } else if (next === SYM_LBRACE) {
        this._match(113);
        const tok3 = this.parseExportSpecifierList();
        this.parseOptComma();
        this._match(117);
        return ["export", tok3];
      } else if (next === SYM_CLASS) {
        const tok2 = this.parseClass();
        return ["export", tok2];
      } else if (next === SYM_DEF) {
        const tok2 = this.parseDef();
        return ["export", tok2];
      } else if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseIdentifier();
        this._match(68);
        const tok4 = this.parseExpression();
        return ["export", ["=", tok2, tok4]];
      } else if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseIdentifier();
        this._match(68);
        this._match(6);
        const tok5 = this.parseExpression();
        return ["export", ["=", tok2, tok5]];
      } else if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseIdentifier();
        this._match(68);
        this._match(36);
        const tok5 = this.parseExpression();
        this._match(38);
        return ["export", ["=", tok2, tok5]];
      } else if (next === SYM_DEFAULT) {
        this._match(129);
        const tok3 = this.parseExpression();
        return ["export-default", tok3];
      } else if (next === SYM_DEFAULT) {
        this._match(129);
        this._match(36);
        const tok4 = this.parseObject();
        this._match(38);
        return ["export-default", tok4];
      } else if (next === SYM_EXPORT_ALL) {
        this._match(133);
        this._match(39);
        const tok4 = this.parseString();
        return ["export-all", tok4];
      } else if (next === SYM_LBRACE) {
        this._match(113);
        this._match(117);
        this._match(39);
        const tok5 = this.parseString();
        return ["export-from", "{}", tok5];
      } else {
        this._match(113);
        const tok3 = this.parseExportSpecifierList();
        this.parseOptComma();
        this._match(117);
        this._match(39);
        const tok7 = this.parseString();
        return ["export-from", tok3, tok7];
      }
    }
    default: this._error([131], this.la.id);
    }
  },

  parseOperation(minPrec = 0) {
    let left = this.parseValue();

    while (this.la && this.la.id !== SYM_EOF) {
      const prec = OPERATOR_PRECEDENCE[this.la.id];
      if (prec === undefined || prec < minPrec) break;

      const op = this.la.id;
      const assoc = OPERATOR_ASSOCIATIVITY[op] || 'left';
      this._match(op);

      // Right-associative: same prec; Left-associative: higher prec
      const nextPrec = prec + (assoc === 'right' ? 0 : 1);
      const right = this.parseOperation(nextPrec);

      // Build AST node with operator name
      const opName = TOKEN_NAMES[op] || op;
      left = [opName, left, right];
    }

    return left;
  },

  parseCode() {
    switch (this.la.id) {
    case SYM_PARAM_START: {
      this._match(95);
            const tok2 = this.parseParamList();
            this._match(96);
            const tok4 = this.parseFuncGlyph();
            const tok5 = this.parseBlock();
            return [tok4, tok2, tok5];
    }
    case SYM_THIN_ARROW: case SYM_FAT_ARROW: {
      const tok1 = this.parseFuncGlyph();
            const tok2 = this.parseBlock();
            return [tok1, [], tok2];
    }
    default: this._error([95, 98, 99], this.la.id);
    }
  },

  parseCodeLine() {
    switch (this.la.id) {
    case SYM_PARAM_START: {
      this._match(95);
            const tok2 = this.parseParamList();
            this._match(96);
            const tok4 = this.parseFuncGlyph();
            const tok5 = this.parseLine();
            return [tok4, tok2, tok5];
    }
    case SYM_THIN_ARROW: case SYM_FAT_ARROW: {
      const tok1 = this.parseFuncGlyph();
            const tok2 = this.parseLine();
            return [tok1, [], tok2];
    }
    default: this._error([95, 98, 99], this.la.id);
    }
  },

  parseExpressionLine() {
    switch (this.la.id) {
    case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: return this.parseCodeLine();
    default: this._error([95, 98, 99], this.la.id);
    }
  },

  parseFuncGlyph() {
    switch (this.la.id) {
    case SYM_THIN_ARROW: return this._match(98);
    case SYM_FAT_ARROW: return this._match(99);
    default: this._error([98, 99], this.la.id);
    }
  },

  parseThrow() {
    switch (this.la.id) {
    case SYM_THROW: {
      this._match(152);
      const next = this._peek();

      if (next === SYM_INDENT) {
        this._match(36);
        const tok3 = this.parseObject();
        this._match(38);
        return ["throw", tok3];
      } else {
        const tok2 = this.parseExpression();
        return ["throw", tok2];
      }
    }
    default: this._error([152], this.la.id);
    }
  },

  parseArguments() {
    switch (this.la.id) {
    case SYM_CALL_START: {
      this._match(29);
      const next = this._peek();

      if (next === SYM_CALL_END) {
        this._match(31);
        return [];
      } else {
        const tok2 = this.parseArgList();
        this.parseOptComma();
        this._match(31);
        return tok2;
      }
    }
    default: this._error([29], this.la.id);
    }
  },

  parseArgList() {
    // Left-recursive: ArgList → Arg | ArgList , Arg
    const items = [this.parseArg()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_COMMA) {
      this._match(SYM_COMMA);
      // FOLLOW(ArgList) = {, ,, CALL_END, TERMINATOR, OUTDENT, INDENT}
      if (this.la.id === SYM_EOF || this.la.id === SYM_CALL_END || this.la.id === SYM_TERMINATOR || this.la.id === SYM_OUTDENT || this.la.id === SYM_INDENT) break;
      items.push(this.parseArg());
    }

    return items;
  },

  parseArg() {
    switch (this.la.id) {
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_WHILE: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_SWITCH: case SYM_IF: case SYM_UNLESS: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC:
      return this.parseExpression();
    case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE:
      return this.parseExpressionLine();
    case SYM_ELLIPSIS: return this.parseSplat();
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 78, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseIf() {
    switch (this.la.id) {
    case SYM_IF: return this.parseIfBlock();
    case SYM_UNLESS: return this.parseUnlessBlock();
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_WHILE: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_SWITCH: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC: {
      const tok1 = this.parseExpression();
            this._match(175);
            const tok3 = this.parseExpression();
            return ["if", tok3, [tok1]];
    }
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseIfBlock() {
    // Left-recursive: IfBlock → IF | IfBlock ELSE IF
    const items = [this.this._match(172)()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_ELSE) {
      this._match(SYM_ELSE);
      // FOLLOW(IfBlock) = {ELSE, $end, TERMINATOR, OUTDENT, ], INDEX_END, CALL_END, PARAM_END, ,, , FOR, }, WHEN, INDENT, .., ..., WHILE, UNTIL, BY, POST_IF, POST_UNLESS, ?, +, -, MATH, **, SHIFT, COMPARE, &, ^, |, &&, ||, ??, !?, RELATION, SPACE?, :, INTERPOLATION_END, ), FOROF, FORIN, FORFROM}
      if (this.la.id === SYM_EOF || this.la.id === SYM_TERMINATOR || this.la.id === SYM_OUTDENT || this.la.id === SYM_RBRACKET || this.la.id === SYM_INDEX_END || this.la.id === SYM_CALL_END || this.la.id === SYM_PARAM_END || this.la.id === SYM_COMMA || this.la.id === SYM_FOR || this.la.id === SYM_RBRACE || this.la.id === SYM_WHEN || this.la.id === SYM_INDENT || this.la.id === SYM_RANGE_INCL || this.la.id === SYM_ELLIPSIS || this.la.id === SYM_WHILE || this.la.id === SYM_UNTIL || this.la.id === SYM_BY || this.la.id === SYM_POST_IF || this.la.id === SYM_POST_UNLESS || this.la.id === SYM_QUESTION || this.la.id === SYM_PLUS || this.la.id === SYM_MINUS || this.la.id === SYM_MATH || this.la.id === SYM_POWER || this.la.id === SYM_SHIFT || this.la.id === SYM_COMPARE || this.la.id === SYM_BITAND || this.la.id === SYM_BITXOR || this.la.id === SYM_BITOR || this.la.id === SYM_AND || this.la.id === SYM_OR || this.la.id === SYM_NULLISH || this.la.id === SYM_OTHERWISE || this.la.id === SYM_RELATION || this.la.id === SYM_TERNARY_Q || this.la.id === SYM_COLON || this.la.id === SYM_INTERPOLATION_END || this.la.id === SYM_RPAREN || this.la.id === SYM_FOROF || this.la.id === SYM_FORIN || this.la.id === SYM_FORFROM) break;
      items.push(this.this._match(172)());
    }

    return items;
  },

  parseUnlessBlock() {
    switch (this.la.id) {
    case SYM_UNLESS: {
      this._match(174);
      const next = this._peek();

      if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START || next === SYM_UNARY || next === SYM_DO || next === SYM_UNARY_MATH || next === SYM_MINUS || next === SYM_PLUS || next === SYM_AWAIT || next === SYM_DEC || next === SYM_INC || next === SYM_TRY || next === SYM_FOR || next === SYM_SWITCH || next === SYM_CLASS || next === SYM_THROW || next === SYM_YIELD || next === SYM_DEF || next === SYM_IF || next === SYM_UNLESS || next === SYM_RETURN || next === SYM_STATEMENT || next === SYM_IMPORT || next === SYM_EXPORT || next === SYM_WHILE || next === SYM_UNTIL || next === SYM_LOOP)) {
        const tok2 = this.parseExpression();
        const tok3 = this.parseBlock();
        return ["unless", tok2, tok3];
      } else {
        const tok2 = this.parseExpression();
        const tok3 = this.parseBlock();
        this._match(168);
        const tok5 = this.parseBlock();
        return ["if", ["!", tok2], tok3, tok5];
      }
    }
    default: this._error([174], this.la.id);
    }
  },

  parseWhile() {
    switch (this.la.id) {
    case SYM_WHILE: case SYM_UNTIL: {
      const tok1 = this.parseWhileSource();
            const tok2 = this.parseBlock();
            return tok1.length === tok2 ? [tok1[0], tok1[tok1], tok2]   : [tok1[0], tok1[tok1], tok1[tok2], tok2];
    }
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_AWAIT: case SYM_SWITCH: case SYM_IF: case SYM_UNLESS: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC: {
      const tok1 = this.parseExpression();
            const tok2 = this.parseWhileSource();
            return tok2.length === tok2 ? [tok2[0], tok2[tok1], [tok1]] : [tok2[0], tok2[tok1], tok2[tok2], [tok1]];
    }
    case SYM_LOOP: return this.parseLoop();
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseWhileSource() {
    switch (this.la.id) {
    case SYM_WHILE: {
      this._match(156);
            const tok2 = this.parseExpression();
            return ["while", tok2];
    }
    case SYM_UNTIL: {
      this._match(157);
            const tok2 = this.parseExpression();
            return ["until", tok2];
    }
    default: this._error([156, 157], this.la.id);
    }
  },

  parseLoop() {
    switch (this.la.id) {
    case SYM_LOOP: {
      this._match(159);
      const next = this._peek();

      if (next === SYM_INDENT) {
        const tok2 = this.parseBlock();
        return ["loop", tok2];
      } else {
        const tok2 = this.parseExpression();
        return ["loop", [tok2]];
      }
    }
    default: this._error([159], this.la.id);
    }
  },

  parseFor() {
    switch (this.la.id) {
    case SYM_FOR: {
      const tok1 = this._match(114);
      const next = this._peek();

      if (next === SYM_OWN) {
        this._match(119);
        const tok3 = this.parseForVariables();
        this._match(116);
        const tok5 = this.parseExpression();
        const tok6 = this.parseBlock();
        return ["for-of"  , tok3, tok5, true, null, tok6];
      } else if (next === SYM_OWN) {
        this._match(119);
        const tok3 = this.parseForVariables();
        this._match(116);
        const tok5 = this.parseExpression();
        this._match(118);
        const tok7 = this.parseExpression();
        const tok8 = this.parseBlock();
        return ["for-of"  , tok3, tok5, true, tok7, tok8];
      } else if (next === SYM_AWAIT) {
        this._match(163);
        const tok3 = this.parseForVariables();
        this._match(162);
        const tok5 = this.parseExpression();
        const tok6 = this.parseBlock();
        return ["for-from", tok3, tok5, true, null, tok6];
      } else if (next === SYM_AWAIT) {
        this._match(163);
        const tok3 = this.parseForVariables();
        this._match(162);
        const tok5 = this.parseExpression();
        this._match(118);
        const tok7 = this.parseExpression();
        const tok8 = this.parseBlock();
        return ["for-from", tok3, tok5, true, tok7, tok8];
      } else if (next === SYM_LBRACKET) {
        const tok2 = this.parseRange();
        const tok3 = this.parseBlock();
        return ["for-in"  , [], tok2, null, null, tok3];
      } else if (next === SYM_LBRACKET) {
        const tok2 = this.parseRange();
        this._match(161);
        const tok4 = this.parseExpression();
        const tok5 = this.parseBlock();
        return ["for-in"  , [], tok2, tok4, null, tok5];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(160);
        const tok5 = this.parseExpression();
        return ["comprehension", tok1, [["for-in"  , tok3, tok5, null]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(160);
        const tok5 = this.parseExpression();
        this._match(118);
        const tok7 = this.parseExpression();
        return ["comprehension", tok1, [["for-in"  , tok3, tok5, null]], [tok7]];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(160);
        const tok5 = this.parseExpression();
        this._match(161);
        const tok7 = this.parseExpression();
        return ["comprehension", tok1, [["for-in"  , tok3, tok5, tok7]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(160);
        const tok5 = this.parseExpression();
        this._match(118);
        const tok7 = this.parseExpression();
        this._match(161);
        const tok9 = this.parseExpression();
        return ["comprehension", tok1, [["for-in"  , tok3, tok5, tok9]], [tok7]];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(160);
        const tok5 = this.parseExpression();
        this._match(161);
        const tok7 = this.parseExpression();
        this._match(118);
        const tok9 = this.parseExpression();
        return ["comprehension", tok1, [["for-in"  , tok3, tok5, tok7]], [tok9]];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(116);
        const tok5 = this.parseExpression();
        return ["comprehension", tok1, [["for-of"  , tok3, tok5, false]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(116);
        const tok5 = this.parseExpression();
        this._match(118);
        const tok7 = this.parseExpression();
        return ["comprehension", tok1, [["for-of"  , tok3, tok5, false]], [tok7]];
      } else if (next === SYM_FOR) {
        this._match(114);
        this._match(119);
        const tok4 = this.parseForVariables();
        this._match(116);
        const tok6 = this.parseExpression();
        return ["comprehension", tok1, [["for-of"  , tok4, tok6, true]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        this._match(119);
        const tok4 = this.parseForVariables();
        this._match(116);
        const tok6 = this.parseExpression();
        this._match(118);
        const tok8 = this.parseExpression();
        return ["comprehension", tok1, [["for-of"  , tok4, tok6, true]], [tok8]];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(162);
        const tok5 = this.parseExpression();
        return ["comprehension", tok1, [["for-from", tok3, tok5, false, null]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseForVariables();
        this._match(162);
        const tok5 = this.parseExpression();
        this._match(118);
        const tok7 = this.parseExpression();
        return ["comprehension", tok1, [["for-from", tok3, tok5, false, null]], [tok7]];
      } else if (next === SYM_FOR) {
        this._match(114);
        this._match(163);
        const tok4 = this.parseForVariables();
        this._match(162);
        const tok6 = this.parseExpression();
        return ["comprehension", tok1, [["for-from", tok4, tok6, true, null]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        this._match(163);
        const tok4 = this.parseForVariables();
        this._match(162);
        const tok6 = this.parseExpression();
        this._match(118);
        const tok8 = this.parseExpression();
        return ["comprehension", tok1, [["for-from", tok4, tok6, true, null]], [tok8]];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseRange();
        return ["comprehension", tok1, [["for-in", [], tok3, null]], []];
      } else if (next === SYM_FOR) {
        this._match(114);
        const tok3 = this.parseRange();
        this._match(161);
        const tok5 = this.parseExpression();
        return ["comprehension", tok1, [["for-in", [], tok3, tok5]], []];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(160);
        const tok4 = this.parseExpression();
        const tok5 = this.parseBlock();
        return ["for-in"  , tok2, tok4, null, null, tok5];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(160);
        const tok4 = this.parseExpression();
        this._match(118);
        const tok6 = this.parseExpression();
        const tok7 = this.parseBlock();
        return ["for-in"  , tok2, tok4, null, tok6, tok7];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(160);
        const tok4 = this.parseExpression();
        this._match(161);
        const tok6 = this.parseExpression();
        const tok7 = this.parseBlock();
        return ["for-in"  , tok2, tok4, tok6, null, tok7];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(160);
        const tok4 = this.parseExpression();
        this._match(118);
        const tok6 = this.parseExpression();
        this._match(161);
        const tok8 = this.parseExpression();
        const tok9 = this.parseBlock();
        return ["for-in"  , tok2, tok4, tok8, tok6, tok9];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(160);
        const tok4 = this.parseExpression();
        this._match(161);
        const tok6 = this.parseExpression();
        this._match(118);
        const tok8 = this.parseExpression();
        const tok9 = this.parseBlock();
        return ["for-in"  , tok2, tok4, tok6, tok8, tok9];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(116);
        const tok4 = this.parseExpression();
        const tok5 = this.parseBlock();
        return ["for-of"  , tok2, tok4, false, null, tok5];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(116);
        const tok4 = this.parseExpression();
        this._match(118);
        const tok6 = this.parseExpression();
        const tok7 = this.parseBlock();
        return ["for-of"  , tok2, tok4, false, tok6, tok7];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE)) {
        const tok2 = this.parseForVariables();
        this._match(162);
        const tok4 = this.parseExpression();
        const tok5 = this.parseBlock();
        return ["for-from", tok2, tok4, false, null, tok5];
      } else {
        const tok2 = this.parseForVariables();
        this._match(162);
        const tok4 = this.parseExpression();
        this._match(118);
        const tok6 = this.parseExpression();
        const tok7 = this.parseBlock();
        return ["for-from", tok2, tok4, false, tok6, tok7];
      }
    }
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseForValue() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: {
      const tok1 = this._match(40);
      const next = this._peek();

      if (next === SYM_ASSIGN) {
        this._match(68);
        const tok3 = this.parseExpression();
        return ["default", tok1, tok3];
      } else {
        return tok1;
      }
    }
    case SYM_LBRACKET: {
      const tok1 = this._match(75);
      const next = this._peek();

      if (next === SYM_ASSIGN) {
        this._match(68);
        const tok3 = this.parseExpression();
        return ["default", tok1, tok3];
      } else {
        return tok1;
      }
    }
    case SYM_AT: {
      const tok1 = this._match(77);
      const next = this._peek();

      if (next === SYM_ASSIGN) {
        this._match(68);
        const tok3 = this.parseExpression();
        return ["default", tok1, tok3];
      } else {
        return tok1;
      }
    }
    case SYM_LBRACE: {
      const tok1 = this._match(113);
      const next = this._peek();

      if (next === SYM_ASSIGN) {
        this._match(68);
        const tok3 = this.parseExpression();
        return ["default", tok1, tok3];
      } else {
        return tok1;
      }
    }
    default: this._error([40, 75, 77, 113], this.la.id);
    }
  },

  parseForVariables() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: {
      const tok1 = this._match(40);
      const next = this._peek();

      if (next === SYM_COMMA) {
        this._match(59);
        const tok3 = this.parseForValue();
        return [tok1, tok3];
      } else {
        return [tok1];
      }
    }
    case SYM_LBRACKET: {
      const tok1 = this._match(75);
      const next = this._peek();

      if (next === SYM_COMMA) {
        this._match(59);
        const tok3 = this.parseForValue();
        return [tok1, tok3];
      } else {
        return [tok1];
      }
    }
    case SYM_AT: {
      const tok1 = this._match(77);
      const next = this._peek();

      if (next === SYM_COMMA) {
        this._match(59);
        const tok3 = this.parseForValue();
        return [tok1, tok3];
      } else {
        return [tok1];
      }
    }
    case SYM_LBRACE: {
      const tok1 = this._match(113);
      const next = this._peek();

      if (next === SYM_COMMA) {
        this._match(59);
        const tok3 = this.parseForValue();
        return [tok1, tok3];
      } else {
        return [tok1];
      }
    }
    default: this._error([40, 75, 77, 113], this.la.id);
    }
  },

  parseTry() {
    switch (this.la.id) {
    case SYM_TRY: {
      this._match(148);
      const next = this._peek();

      if (next === SYM_INDENT) {
        const tok2 = this.parseBlock();
        return ["try", tok2];
      } else if (next === SYM_INDENT) {
        const tok2 = this.parseBlock();
        const tok3 = this.parseCatch();
        return ["try", tok2, tok3];
      } else if (next === SYM_INDENT) {
        const tok2 = this.parseBlock();
        this._match(150);
        const tok4 = this.parseBlock();
        return ["try", tok2, tok4];
      } else {
        const tok2 = this.parseBlock();
        const tok3 = this.parseCatch();
        this._match(150);
        const tok5 = this.parseBlock();
        return ["try", tok2, tok3, tok5];
      }
    }
    default: this._error([148], this.la.id);
    }
  },

  parseCatch() {
    switch (this.la.id) {
    case SYM_CATCH: {
      this._match(151);
      const next = this._peek();

      if (next === SYM_IDENTIFIER) {
        const tok2 = this.parseIdentifier();
        const tok3 = this.parseBlock();
        return [tok2, tok3];
      } else if (next === SYM_LBRACE) {
        const tok2 = this.parseObject();
        const tok3 = this.parseBlock();
        return [tok2, tok3];
      } else {
        const tok2 = this.parseBlock();
        return [null, tok2];
      }
    }
    default: this._error([151], this.la.id);
    }
  },

  parseSwitch() {
    switch (this.la.id) {
    case SYM_SWITCH: {
      this._match(166);
      const next = this._peek();

      if (next === SYM_INDENT) {
        this._match(36);
        const tok3 = this.parseWhens();
        this._match(38);
        return ["switch", null, tok3, null];
      } else if (next === SYM_INDENT) {
        this._match(36);
        const tok3 = this.parseWhens();
        this._match(168);
        const tok5 = this.parseBlock();
        this._match(38);
        return ["switch", null, tok3, tok5];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START || next === SYM_UNARY || next === SYM_DO || next === SYM_UNARY_MATH || next === SYM_MINUS || next === SYM_PLUS || next === SYM_AWAIT || next === SYM_DEC || next === SYM_INC || next === SYM_TRY || next === SYM_FOR || next === SYM_SWITCH || next === SYM_CLASS || next === SYM_THROW || next === SYM_YIELD || next === SYM_DEF || next === SYM_IF || next === SYM_UNLESS || next === SYM_RETURN || next === SYM_STATEMENT || next === SYM_IMPORT || next === SYM_EXPORT || next === SYM_WHILE || next === SYM_UNTIL || next === SYM_LOOP)) {
        const tok2 = this.parseExpression();
        this._match(36);
        const tok4 = this.parseWhens();
        this._match(38);
        return ["switch", tok2, tok4, null];
      } else {
        const tok2 = this.parseExpression();
        this._match(36);
        const tok4 = this.parseWhens();
        this._match(168);
        const tok6 = this.parseBlock();
        this._match(38);
        return ["switch", tok2, tok4, tok6];
      }
    }
    default: this._error([166], this.la.id);
    }
  },

  parseWhens() {
    switch (this.la.id) {
    case SYM_LEADING_WHEN: {
      const tok1 = this._match(170);
      const next = this._peek();

      if (next === SYM_LEADING_WHEN) {
        const tok2 = this.parseWhen();
        return [...tok1, tok2];
      } else {
        return [tok1];
      }
    }
    default: this._error([170], this.la.id);
    }
  },

  parseWhen() {
    switch (this.la.id) {
    case SYM_LEADING_WHEN: {
      this._match(170);
      const next = this._peek();

      if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_JS || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_LBRACKET || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_STRING || next === SYM_STRING_START || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNARY || next === SYM_DO || next === SYM_UNARY_MATH || next === SYM_MINUS || next === SYM_PLUS || next === SYM_AWAIT || next === SYM_DEC || next === SYM_INC || next === SYM_IF || next === SYM_UNLESS || next === SYM_STATEMENT || next === SYM_TRY || next === SYM_WHILE || next === SYM_UNTIL || next === SYM_LOOP || next === SYM_FOR || next === SYM_SWITCH || next === SYM_CLASS || next === SYM_THROW || next === SYM_YIELD || next === SYM_DEF || next === SYM_RETURN || next === SYM_IMPORT || next === SYM_EXPORT)) {
        const tok2 = this.parseSimpleArgs();
        const tok3 = this.parseBlock();
        return ["when", tok2, tok3];
      } else {
        const tok2 = this.parseSimpleArgs();
        const tok3 = this.parseBlock();
        this._match(6);
        return ["when", tok2, tok3];
      }
    }
    default: this._error([170], this.la.id);
    }
  },

  parseClass() {
    switch (this.la.id) {
    case SYM_CLASS: {
      this._match(121);
      const next = this._peek();

      if (next === SYM_INDENT) {
        const tok2 = this.parseBlock();
        return ["class", null, null, tok2];
      } else if (next === SYM_EXTENDS) {
        this._match(122);
        const tok3 = this.parseExpression();
        return ["class", null, tok3];
      } else if (next === SYM_EXTENDS) {
        this._match(122);
        const tok3 = this.parseExpression();
        const tok4 = this.parseBlock();
        return ["class", null, tok3, tok4];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseSimpleAssignable();
        return ["class", tok2, null];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseSimpleAssignable();
        const tok3 = this.parseBlock();
        return ["class", tok2, null, tok3];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseSimpleAssignable();
        this._match(122);
        const tok4 = this.parseExpression();
        return ["class", tok2, tok4];
      } else if ((next === SYM_IDENTIFIER || next === SYM_AT || next === SYM_LBRACKET || next === SYM_LBRACE || next === SYM_NUMBER || next === SYM_JS || next === SYM_REGEX || next === SYM_REGEX_START || next === SYM_UNDEFINED || next === SYM_NULL || next === SYM_BOOL || next === SYM_INFINITY || next === SYM_NAN || next === SYM_LPAREN || next === SYM_SUPER || next === SYM_DYNAMIC_IMPORT || next === SYM_DO_IIFE || next === SYM_THIS || next === SYM_NEW_TARGET || next === SYM_IMPORT_META || next === SYM_PARAM_START || next === SYM_THIN_ARROW || next === SYM_FAT_ARROW || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseSimpleAssignable();
        this._match(122);
        const tok4 = this.parseExpression();
        const tok5 = this.parseBlock();
        return ["class", tok2, tok4, tok5];
      } else {
        return ["class", null, null];
      }
    }
    default: this._error([121], this.la.id);
    }
  },

  parseAssign() {
    switch (this.la.id) {
    case SYM_LBRACKET: {
      const tok1 = this._match(75);
      const next = this._peek();

      if (next === SYM_ASSIGN) {
        this._match(68);
        const tok3 = this.parseExpression();
        return ["=", tok1, tok3];
      } else if (next === SYM_ASSIGN) {
        this._match(68);
        this._match(6);
        const tok4 = this.parseExpression();
        return ["=", tok1, tok4];
      } else {
        this._match(68);
        this._match(36);
        const tok4 = this.parseExpression();
        this._match(38);
        return ["=", tok1, tok4];
      }
    }
    case SYM_LBRACE: {
      const tok1 = this._match(113);
      const next = this._peek();

      if (next === SYM_ASSIGN) {
        this._match(68);
        const tok3 = this.parseExpression();
        return ["=", tok1, tok3];
      } else if (next === SYM_ASSIGN) {
        this._match(68);
        this._match(6);
        const tok4 = this.parseExpression();
        return ["=", tok1, tok4];
      } else {
        this._match(68);
        this._match(36);
        const tok4 = this.parseExpression();
        this._match(38);
        return ["=", tok1, tok4];
      }
    }
    default: this._error([40, 75, 77, 113], this.la.id);
    }
  },

  parseAssignObj() {
    switch (this.la.id) {
    case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_LBRACKET: case SYM_AT:
      return [/* ERROR: ObjAssignable skipped */, 1, null];
    case SYM_ELLIPSIS: return this.parseObjRestValue();
    default: this._error([40, 42, 44, 46, 47, 75, 77, 78], this.la.id);
    }
  },

  parseAssignList() {
    // Left-recursive: AssignList →  | AssignList , AssignObj
    const items = [this.this._match(undefined)()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_COMMA) {
      this._match(SYM_COMMA);
      // FOLLOW(AssignList) = {, ,, }, TERMINATOR, INDENT, OUTDENT}
      if (this.la.id === SYM_EOF || this.la.id === SYM_RBRACE || this.la.id === SYM_TERMINATOR || this.la.id === SYM_INDENT || this.la.id === SYM_OUTDENT) break;
      items.push(this.parseAssignObj());
    }

    return items;
  },

  parseParam() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: case SYM_LBRACKET: case SYM_AT: case SYM_LBRACE: return this.parseParamVar();
    case SYM_ELLIPSIS: {
      this._match(78);
            const tok2 = this.parseParamVar();
            return ["rest", tok2];
    }
    default: this._error([40, 75, 77, 78, 113], this.la.id);
    }
  },

  parseParamList() {
    // Left-recursive: ParamList →  | ParamList , Param
    const items = [this.this._match(undefined)()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_COMMA) {
      this._match(SYM_COMMA);
      // FOLLOW(ParamList) = {CALL_END, PARAM_END, ,, , TERMINATOR, INDENT, OUTDENT}
      if (this.la.id === SYM_EOF || this.la.id === SYM_CALL_END || this.la.id === SYM_PARAM_END || this.la.id === SYM_TERMINATOR || this.la.id === SYM_INDENT || this.la.id === SYM_OUTDENT) break;
      items.push(this.parseParam());
    }

    return items;
  },

  parseParamVar() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: return this.parseIdentifier();
    case SYM_AT: return this.parseThisProperty();
    case SYM_LBRACKET: return this.parseArray();
    case SYM_LBRACE: return this.parseObject();
    default: this._error([40, 75, 77, 113], this.la.id);
    }
  },

  parseSplat() {
    switch (this.la.id) {
    case SYM_ELLIPSIS: {
      this._match(78);
            const tok2 = this.parseExpression();
            return ["...", tok2];
    }
    default: this._error([78], this.la.id);
    }
  },

  parseSlice() {
    switch (this.la.id) {
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_WHILE: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_SWITCH: case SYM_IF: case SYM_UNLESS: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC: {
      const tok1 = this.parseExpression();
            const tok2 = this.parseRangeDots();
            const tok3 = this.parseExpression();
            return [tok2, tok1, tok3];
    }
    case SYM_ELLIPSIS: case SYM_RANGE_INCL: {
      const tok1 = this.parseRangeDots();
            const tok2 = this.parseExpression();
            return [tok1, null, tok2];
    }
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 78, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 143, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseForVar() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: return this.parseIdentifier();
    case SYM_AT: return this.parseThisProperty();
    case SYM_LBRACKET: return this.parseArray();
    case SYM_LBRACE: return this.parseObject();
    default: this._error([40, 75, 77, 113], this.la.id);
    }
  },

  parseOptFuncExist() {
    switch (this.la.id) {
    case SYM_FUNC_EXIST: return this._match(136);
    default: return [];
    }
  },

  parseOptComma() {
    switch (this.la.id) {
    case SYM_COMMA: return this._match(59);
    default: return [];
    }
  },

  parseOptElisions() {
    switch (this.la.id) {
    case SYM_COMMA: {
      this._match(59);
      const next = this._peek();

      if (next === SYM_COMMA) {
        const tok2 = this.parseElisions();
        return [...tok2];
      } else {
        return [];
      }
    }
    default: this._error([59], this.la.id);
    }
  },

  parseElision() {
    switch (this.la.id) {
    case SYM_COMMA: {
      const tok1 = this._match(59);
      const next = this._peek();

      if (next === SYM_TERMINATOR) {
        this._match(6);
        return tok1;
      } else {
        return null;
      }
    }
    default: this._error([59], this.la.id);
    }
  },

  parseElisions() {
    switch (this.la.id) {
    case SYM_COMMA: {
      const tok1 = this._match(59);
      const next = this._peek();

      if (next === SYM_COMMA) {
        const tok2 = this.parseElision();
        return [...tok1, tok2];
      } else {
        return [tok1];
      }
    }
    default: this._error([59], this.la.id);
    }
  },

  parseObjRestValue() {
    switch (this.la.id) {
    case SYM_ELLIPSIS: {
      this._match(78);
      const next = this._peek();

      if ((next === SYM_IDENTIFIER || next === SYM_PROPERTY || next === SYM_AT)) {
        const tok2 = this.parseSimpleObjAssignable();
        return ["...", tok2];
      } else {
        const tok2 = this.parseObjSpreadExpr();
        return ["...", tok2];
      }
    }
    default: this._error([78], this.la.id);
    }
  },

  parseSimpleArgs() {
    // Left-recursive: SimpleArgs → Expression | SimpleArgs , Expression
    const items = [this.parseExpression()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_COMMA) {
      this._match(SYM_COMMA);
      // FOLLOW(SimpleArgs) = {,, INDENT}
      if (this.la.id === SYM_EOF || this.la.id === SYM_INDENT) break;
      items.push(this.parseExpression());
    }

    return items;
  },

  parseArgElision() {
    switch (this.la.id) {
    case SYM_STATEMENT: case SYM_DEF: case SYM_YIELD: case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_ELLIPSIS: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_RETURN: case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_FOR: case SYM_CLASS: case SYM_IMPORT: case SYM_EXPORT: case SYM_THIS: case SYM_TRY: case SYM_THROW: case SYM_LPAREN: case SYM_WHILE: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_SWITCH: case SYM_IF: case SYM_UNLESS: case SYM_UNARY: case SYM_DO: case SYM_DO_IIFE: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC:
      return [this.parseArg()];
    case SYM_COMMA: {
      const tok1 = this.parseElisions();
            const tok2 = this.parseArg();
            return [...tok1, tok2];
    }
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 59, 61, 62, 63, 64, 65, 66, 75, 77, 78, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

  parseArgElisionList() {
    // Left-recursive: ArgElisionList → ArgElision | ArgElisionList , ArgElision
    const items = [this.parseArgElision()];

    // Iteration with FOLLOW-based termination
    while (this.la && this.la.id === SYM_COMMA) {
      this._match(SYM_COMMA);
      // FOLLOW(ArgElisionList) = {, ,, ], TERMINATOR, OUTDENT, INDENT}
      if (this.la.id === SYM_EOF || this.la.id === SYM_RBRACKET || this.la.id === SYM_TERMINATOR || this.la.id === SYM_OUTDENT || this.la.id === SYM_INDENT) break;
      items.push(this.parseArgElision());
    }

    return items;
  },

  parseOptElisions() {
    switch (this.la.id) {
    case SYM_COMMA: {
      this._match(59);
      const next = this._peek();

      if (next === SYM_COMMA) {
        const tok2 = this.parseElisions();
        return [...tok2];
      } else {
        return [];
      }
    }
    default: this._error([59], this.la.id);
    }
  },

  parseInterpolations() {
    switch (this.la.id) {
    case SYM_STRING: {
      const tok1 = this._match(46);
      const next = this._peek();

      if ((next === SYM_INTERPOLATION_START || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseInterpolationChunk();
        return [...tok1, tok2];
      } else {
        return [tok1];
      }
    }
    case SYM_STRING_START: {
      const tok1 = this._match(47);
      const next = this._peek();

      if ((next === SYM_INTERPOLATION_START || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseInterpolationChunk();
        return [...tok1, tok2];
      } else {
        return [tok1];
      }
    }
    case SYM_INTERPOLATION_START: {
      const tok1 = this._match(51);
      const next = this._peek();

      if ((next === SYM_INTERPOLATION_START || next === SYM_STRING || next === SYM_STRING_START)) {
        const tok2 = this.parseInterpolationChunk();
        return [...tok1, tok2];
      } else {
        return [tok1];
      }
    }
    default: this._error([46, 47, 51], this.la.id);
    }
  },

  parseInterpolationChunk() {
    switch (this.la.id) {
    case SYM_INTERPOLATION_START: {
      this._match(51);
            const tok2 = this.parseBody();
            this._match(52);
            return tok2;
    }
    case SYM_STRING: case SYM_STRING_START: return this.parseString();
    default: this._error([46, 47, 51], this.la.id);
    }
  },

  parseExpression() {
    switch (this.la.id) {
    case SYM_IDENTIFIER: case SYM_NUMBER: case SYM_STRING: case SYM_STRING_START: case SYM_REGEX: case SYM_REGEX_START: case SYM_JS: case SYM_UNDEFINED: case SYM_NULL: case SYM_BOOL: case SYM_INFINITY: case SYM_NAN: case SYM_LBRACKET: case SYM_AT: case SYM_SUPER: case SYM_DYNAMIC_IMPORT: case SYM_NEW_TARGET: case SYM_IMPORT_META: case SYM_LBRACE: case SYM_THIS: case SYM_LPAREN: case SYM_DO_IIFE:
      return this.parseValue();
    case SYM_PARAM_START: case SYM_THIN_ARROW: case SYM_FAT_ARROW: return this.parseCode();
    case SYM_STATEMENT: case SYM_RETURN: case SYM_IMPORT: case SYM_EXPORT: case SYM_UNTIL: case SYM_LOOP: case SYM_AWAIT: case SYM_UNLESS: case SYM_UNARY: case SYM_DO: case SYM_UNARY_MATH: case SYM_MINUS: case SYM_PLUS: case SYM_DEC: case SYM_INC:
      return this.parseOperation(0);
    case SYM_IF: return this.parseIf();
    case SYM_TRY: return this.parseTry();
    case SYM_WHILE: return this.parseWhile();
    case SYM_FOR: return this.parseFor();
    case SYM_SWITCH: return this.parseSwitch();
    case SYM_CLASS: return this.parseClass();
    case SYM_THROW: return this.parseThrow();
    case SYM_YIELD: return this.parseYield();
    case SYM_DEF: return this.parseDef();
    default: this._error([11, 27, 35, 40, 44, 46, 47, 54, 55, 61, 62, 63, 64, 65, 66, 75, 77, 83, 86, 94, 95, 98, 99, 111, 112, 113, 114, 121, 123, 131, 138, 148, 152, 153, 156, 157, 159, 163, 166, 172, 174, 177, 178, 179, 180, 181, 182, 183, 184], this.la.id);
    }
  },

};  // End of parser object

export { parser }
export const parse = parser.parse.bind(parser)
export default parser