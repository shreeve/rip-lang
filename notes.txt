
Review the code to ensure it's correct, clear, consistent, concise, and efficient?

==

Follow the official workflow:

• Create a GitHub issue
• Create a new git branch
• Create several tests, which should all fail
• Fix the problem
• When done, run the tests, to show it's passing
• Bump the patch version (the z in x.y.z) of package.json
• Run "bun run parser && bun run browser"
• Add an entry to the CHANGELOG.md file
• Commit and push to GitHub
• Create a GitHub PR, merge it, then close the ticket.
• Do not push to GitHub yet

==

Follow the surgical workflow:

• Create several tests, which should all fail
• Fix the problem
• When done, run the tests, to show it's passing
• Bump the version's patch number (the z in x.y.z) of package.json
• Run "bun run parser && bun run browser"
• Add an entry to the CHANGELOG.md file
• Commit and push to GitHub

==

Release a new version of Rip:

• bump the patch number (the z in x.y.z) of package.json
• bun run parser && bun run browser
• update the CHANGELOG.md file to bring it current
• commit and push
• npm publish

==

Update the AGENT.md file with all the information a new AI needs to get fully
up to speed on this project and to begin working immediately. Also, update the
README.md file to provide a GitHub developer with a quick and informative
overview of this project, why it exists, what problems it solves, and how to
use for that purpose.

==

Can you DEEPLY analyze codegen.js to see if there are areas that we can
optimize? Is there dead code? Functions that are never called? Are there
functions that do things the regex/string processing way, when they could
also be doing s-expression work? Are there ways to reduce the number of
lines of code in codegen.js to make it snappier and more succinct? Are
there major or minor refactorings that we could do? Should the code be
organized differently? What would you do to make codegen.js be a
MODEL FOR BEAUTIFUL AND EFFICIENT SOFTWARE?

Do one DEEP DIVE PASS on the whole file to see if there are areas where
it can be optimized or improved? Have we eliminated all (or most of?)
the string processing and replaced with s-expression based logic and
processing? Are there areas of redundancy or code that is not being used?
Can we use slightly different algorithms in some places that would make
things more efficient? What recommendations do you have with this code?
Does it seem high quality and clean?

==

Rip's heredocs (''' non-interpolating, """ interpolating) support visual margin control: when the closing delimiter is preceded only by whitespace, its column position becomes the "left margin" for all content lines. Text indented to the right of this baseline retains its relative indentation, while text at or left of the baseline is stripped to match it. This makes code generation elegant—just position the closing ''' or """ where you want the baseline, and the output aligns perfectly without manual indentation management.

• Two heredoc types: ''' (literal) and """ (interpolating)
• Visual margin control: Closing delimiter's column = left margin baseline
• Smart stripping: Only when closing delimiter has only whitespace before it
• Relative indentation preserved: Text right of baseline keeps its indent offset
• Perfect for codegen: Position closing delimiter → instant clean alignment
• Example: Closing at column 2 strips 2 spaces from all lines, closing at column 0 preserves all indentation

--

Actions in the grammar are simple.

1) empty value means use token 1 (1-indexed)
2) "" or null means use those as-is
3) a number 1 or 3 means use token 1 or 3
4) a string with numbers will be converted to the token position
5) if a bare number should be protected, then token numbers use a $n and bare numbers are untouched

--

Grammar actions in Rip use a simple, intuitive indexing system: empty/undefined defaults to token 1 (first matched symbol), literal "" or null pass through as-is, bare numbers (1, 3, etc.) reference token positions (1-indexed), and strings containing numbers automatically convert to token position references. For strings needing literal numbers, use $n syntax for token references (e.g., $1, $3) while bare numbers remain untouched—this prevents ambiguity between "return token 2" versus "return the number 2".

• Default (empty/undefined): Returns token 1 (first matched symbol)
• Literal values: "" or null pass through unchanged
• Bare numbers: 1, 3, etc. reference token positions (1-indexed)
• String actions: Numbers in strings auto-convert to token positions
• Protected literals: Use $n syntax ($1, $3) for tokens; allows bare numbers to stay literal

Rule examples with their action strings:

['Token1 Token2 Token3'] => action is Token1
['Token1 Token2 Token3', 'null'] => action is 'null' (pass literal value)
['Token1 Token2 Token3', 3] => action is Token3 (number -> token number)
['Token1 Token2 Token3', '2'] => action is Token2 (string -> token number)
['Token1 Token2 Token3', '[1, 3, 2]'] => action is [Token1, Token3, Token2] (array -> array of tokens)
['Token1 Token2 Token3', '[$3.slice(3,7), $2, 12]'] => action is [Token3.slice(3,7), Token2, 12] (same, protect bare numbers)

==

Lexer+Rewriter
    1943    8247   75721 lexer.js
    1138    5765   49855 rewriter.js
     477    2073   16702 helpers.js

Compiler
     187     846    6131 scope.js
     229    1023    8585 sourcemap.js
    9204   33365  327152 nodes.js
     217     908    7991 index.js
     409    1905   16476 coffeescript.js
     100     387    3557 register.js

Tools
     230    1067    8423 optparse.js
     750    2939   25896 command.js
     151     709    5613 browser.js
     135     558    4493 cake.js
     305    1189   11070 repl.js

    2496    6603   64357 grammar.js
    1343    4966  192198 parser.js

   19314   72550  824220 total

==

Lexer+Rewriter (1 file):
    3145   14810  123696 src/lexer.js

Compiler (2 files):
     149     575    4768 src/compiler.js
    4808   21188  191464 src/codegen.js

Tools (2 files):
      97     347    2821 src/browser.js
     423    1256   11969 src/repl.js

Totals:
    8973   40498  620883 total

===

# this source...
code = '''
    foo(bar) {
      console.log(bar);
    }
'''

# produces this js...
code = `    foo(bar) {
      console.log(bar);
    }`;

# and this...
code = '''
    foo(bar) {
      console.log(bar);
    }
  '''

# produces this...
code = `  foo(bar) {
    console.log(bar);
  }`;

Grammar actions in Rip use a simple, intuitive indexing system: empty/undefined defaults to token 1 (first matched symbol), literal "" or null pass through as-is, bare numbers (1, 3, etc.) reference token positions (1-indexed), and strings containing numbers automatically convert to token position references. For strings needing literal numbers, use $n syntax for token references (e.g., $1, $3) while bare numbers remain untouched—this prevents ambiguity between "return token 2" versus "return the number 2".

• Default (empty/undefined): Returns token 1 (first matched symbol)
• Literal values: "" or null pass through unchanged
• Bare numbers: 1, 3, etc. reference token positions (1-indexed)
• String actions: Numbers in strings auto-convert to token positions
• Protected literals: Use $n syntax ($1, $3) for tokens; allows bare numbers to stay literal

Examples:

['Token1 Token2 Token3'] => action is Token1
['Token1 Token2 Token3', 'null'] => action is 'null'
['Token1 Token2 Token3', 3] => action is Token3
['Token1 Token2 Token3', '2'] => action is Token2
['Token1 Token2 Token3', '[1, 3, 2]'] => action is [Token1, Token3, Token2]
['Token1 Token2 Token3', '[$3.slice(3,7), $2, 12]'] => action is [Token3.slice(3,7), Token2, 12]

_normalizeActionForPRD: (action, symbols) ->
  return "$1" if not action or action is 1
  return "$#{action}" if typeof action is 'number'
  action = String(action)
  /\$\d+/.test(action) ? action : action.replace /(\d+)/g, '$$$1'

==

PLUGGABLE LANGUAGE ARCHITECTURE (Future Enhancement)

The parser.js file essentially describes the Rip language in a compressed form
(268KB parseTable is 92% of the file). This could be split into:

Generic Components (reusable across languages):
  src/solar.rip          # Parser generator engine (already generic!)
  src/parser.js          # Generic LR(1) parsing algorithm (~6KB)
  src/compiler.js        # Thin orchestrator (loads language plugins)

Language-Specific Components:
  src/lang/rip/lexer.js     # Tokenization rules (3,145 LOC)
  src/lang/rip/grammar.rip  # Grammar definition (source)
  src/lang/rip/syntax.js    # Parse tables (GENERATED, 268KB)
  src/lang/rip/codegen.js   # Code generation (5,265 LOC)

Benefits:
  • Pluggable languages (CoffeeScript, TypeScript-lite, etc.)
  • Smaller individual files (runtime is tiny, languages separate)
  • Clear separation of concerns (algorithm vs data)
  • Better testability (test parser algorithm independently)
  • Makes Solar a true multi-language parser toolkit

syntax.js contains:
  • symbolIds, tokenNames (metadata)
  • parseTable (the huge one - 268KB)
  • ruleTable, ruleActions (execution logic)

Build: solar grammar.rip -o lang/rip/syntax.js
