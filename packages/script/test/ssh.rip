import Script from '../script.rip'

chat = Script.ssh! 'trust',
  cols: process.stdout.columns ?? 120
  rows: process.stdout.rows    ?? 32

chat! [
  '~> '  , "mc"
  'Hint:', [:pure, "\x1b0"]
  '~> '  , "exit"
]

chat.disconnect!
