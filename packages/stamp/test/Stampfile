# Test Stampfile

set NAME  testhost
set POOL  tank

packages curl git jq

user shreeve 1100:1100
  groups trust

group jail 1002

firewall
  default deny incoming
  default allow outgoing
  allow ssh
