# Prompt for Next AI Session

## Context

You're continuing development of a clean-room Predictive Recursive Descent (PRD) parser generator for the Rip language. Previous session built infrastructure and discovered a breakthrough approach but hit runtime issues.

## Current State (Commit: 1ac7ddb)

**Working:**
- ✅ Clean infrastructure: ~500 lines of PRD generation code
- ✅ Left-recursion detection → iterative loops  
- ✅ Action compilation (simple & elegant)
- ✅ Symbolic constants (readable generated code)
- ✅ **Oracle-informed routing via FIRST sets** (breakthrough!)
- ✅ All 86 parse functions generate

**Not Working:**
- ❌ Runtime hangs when testing `echo '42' | ./bin/rip -s`
- ❌ Infinite loop somewhere (doesn't consume tokens or has deeper cycle)

## The Breakthrough: Oracle-Informed Routing

**Theory:** Use FIRST sets (already computed by SLR(1) analysis) to generate cycle-free routing at generation time.

**Example:**
```coffee
Expression: [
  o 'Value'     # FIRST([Value]) = {NUMBER, STRING, IDENTIFIER, ...}
  o 'For'       # FIRST([For]) = {FOR}  
  o 'While'     # FIRST([While]) = {WHILE}
]

# Generate:
parseExpression() {
  switch (this.la?.id) {
    case SYM_NUMBER: return this.parseValue();   // Oracle decided!
    case SYM_FOR: return this.parseFor();
    case SYM_WHILE: return this.parseWhile();
  }
}
```

**No cycles because:** FIRST sets are disjoint, each token routes to ONE alternative.

## The Current Problem

Parser generates but hangs at runtime. Need to debug:

1. **Token consumption:** Are tokens being consumed?
2. **Parse chain:** Does parseBody → parseLine → parseExpression → parseValue → parseLiteral → parseAlphaNumeric work?
3. **Terminal handling:** Does parseAlphaNumeric match NUMBER token correctly?
4. **Deeper cycles:** Is there another cycle we haven't caught?

## Your Mission

**Debug the runtime hang and get `echo '42' | ./bin/rip -s` working.**

### Debugging Approach

**Step 1: Add trace logging**
```coffee
# In generated parse() method:
console.log('Token:', this.la.id, this.la.value);

# In generated parseXXX() methods:
console.log('Entering parseXXX, la:', this.la.id);
```

**Step 2: Test and trace**
```bash
echo '42' | timeout 3 ./bin/rip -s 2>&1
# Watch the trace to see where it loops
```

**Step 3: Find the issue**
- Which function loops?
- Does it consume tokens?
- Does try/catch block throw and retry infinitely?
- Is oracle routing working?

**Step 4: Fix**
- Might be: token stream building
- Might be: try/catch in non-oracle functions
- Might be: terminal handling in oracle dispatch
- Might be: action returns undefined instead of value

## Key Files

1. **src/grammar/solar.rip** (1,471 lines) - Current implementation
   - Lines 742-780: Oracle routing extraction (_extractOracleRouting)
   - Lines 989-1030: Oracle dispatch generation (_generateOracleDispatch)
   - Lines 1003-1056: Iterative loops (_generateIterative)

2. **src/grammar/grammar.rip** (808 lines) - The grammar
   - Line 14-17: Root rules
   - Line 20-24: Body rules (left-recursive)
   - Line 101-105: AlphaNumeric rules (NUMBER terminal)

3. **src/grammar/solar-old.rip** (4,551 lines) - Working reference
   - Use for comparison ONLY
   - Don't copy bulk code
   - Reference for understanding patterns

4. **PLAN.md** - Complete strategy document
5. **notes/prd-patterns.md** - 21 generic fixes
6. **notes/failure-analysis.md** - 7 failing test analysis

## Success Criteria

**Immediate goal:** `echo '42' | ./bin/rip -s` outputs:
```
(program
  42
)
```

**Once working:**
- Test `echo 'x = 42' | ./bin/rip -s`
- Expand incrementally to all inputs
- Target: 962/962 tests passing

## Philosophy

**MATCH GRAMMAR → EMIT SEXPS**

Keep it simple. The table-driven parser is 350 lines and works perfectly. PRD should be similar - maybe 1,000-2,000 lines total.

Don't over-engineer. Fix the runtime issue, get tests passing, iterate.

## Important Notes

- ✅ Infrastructure is solid (don't rebuild)
- ✅ Theory is sound (FIRST-based routing works)
- ⚠️ Need runtime debugging (systematic, not guess-and-check)
- 🎯 Goal: Minimal working parser, then expand

**This is a publishable approach if we can get it working!** Novel oracle-informed PRD generation using FIRST sets. Good luck! 🚀

